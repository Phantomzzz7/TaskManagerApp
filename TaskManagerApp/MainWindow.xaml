<Window x:Class="TaskManagerApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskManagerApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBox x:Name="TitleTextBox" Width="250" Height="30" VerticalContentAlignment="Center"/>
                <Button x:Name="AddTaskButton" Content="Add Task" Width="100" Height="30" Margin="10,0,0,0" Click="AddTaskButton_Click"/>
            </StackPanel>
            <TextBlock Grid.Row="1" Text="Tasks:" FontWeight="Bold" Margin="0,0,0,5"/>
            <ListBox x:Name="TasksListBox" Grid.Row="2">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox Content="{Binding Title}"
                      IsChecked="{Binding IsCompleted, Mode=TwoWay}"
                      Checked="TaskCheckBox_Changed"
                      Unchecked="TaskCheckBox_Changed"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</Window>
